lo.std{
  integer <~ thing.
  float <~ thing.
  string <~ thing.

  ss ::= ssSp | ssNl | ssStr(string) | ssSeq(list[ss]) | ssIndent(integer,ss).

  thing <~ {
    show : ()=>ss.
  }.

  thing:thing.
  thing..{
    show() => ssStr("thing").
  }.
}