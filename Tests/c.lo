test.c{
  ss ::= ss(string) | sc(integer) | ssSeq(list[ss]).

  all t ~~ list[t] ::= '[]' | ',..'(t,list[t]).

  public
  all t ~~ option[t] ::= none | some(t).

  x : option[integer].
  x = some(1).
}
