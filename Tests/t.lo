test.t{
  import lo.

  type alpha = (string,integer).

  type all x ~~ beta[x] = (string,x).

  type all x, y/2 ~~ alphabeta[x,y] <~ {}.

/*  f:alpha.
  f=("f",3).
*/
  g:beta[integer].
  g=("g",4).
}
